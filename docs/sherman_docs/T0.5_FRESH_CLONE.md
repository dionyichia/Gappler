# T0.5: Fresh-Clone Bench Evidence

**Session 2026-09-21, Sherman's Mac.**

**Shared task:** [`../PROJECT_PLAN.md`](../PROJECT_PLAN.md), T0.5.
**Record role:** Sherman-owned evidence. The shared plan remains authoritative for task status.

## Objective

Show that a clean clone of `main` can run the repository's safe bench without relying on state from
Sherman's working checkout.

## Method

Cloned `https://github.com/dionyichia/Gappler.git`, branch `main`, into an isolated temporary path:

```text
/var/folders/fl/74q2tc_x68s9z_2m014tnxww0000gn/T/opencode/gappler-t0.5
```

The fresh clone resolved to `9bbb26a6499adce5b717eccb43130cc1e65d3c10`.

Ran:

```bash
./bench/run.sh
```

No ROS nodes, drivers, simulators, or hardware-control processes were started.

## Result

| Level | Result | Meaning |
|---|---|---|
| L0 static checks | PASS | 0 findings in code owned by the project; 2 vendor-code findings only |
| L1 contracts | PASS | 0 contract regressions against baseline `1461e72` |
| L2 preflight | PASS | Mac correctly identified as a non-lab host: 10 pass, 0 fail, 0 warn, 27 expected skips |
| L3 build | SKIPPED | No ROS 2 Humble or colcon on the Mac |
| L4 simulation | SKIPPED | Requires the lab box environment |
| L5 hardware | Not run | Requires a person at the robot and is never automated |

## Interpretation

`[observed]` The repository's portable, safe checks work from a clean Mac clone at `9bbb26a`.
This is Sherman's half of T0.5. It does not establish ROS build portability, nor does it prove robot
hardware behavior. An independent clone result remains required before T0.5 can close.

## Orin Attempt

`[observed]` An independent full clone was attempted on `nvidia-desktop` on 2026-09-21. It did not
complete, so no Orin bench ran.

| Attempt | Result |
|---|---|
| Full clone | Exceeded two minutes before completion |
| Shallow clone | GitHub HTTP/2 RPC disconnect: `curl 92`, early EOF |
| Shallow HTTP/1.1 clone | GitHub transport disconnect: `curl 56`, early EOF |
| Partial clone | GitHub HTTP/2 RPC disconnect before checkout |

The incomplete directories remain on the Orin and must not be treated as test evidence. Resolve the
Orin-to-GitHub transfer problem, then create a new clean clone and repeat this benchmark.

## Changelog

| Date | Who | Change |
|---|---|---|
| 2026-09-21 | OpenCode (GPT-5.6 Terra) + Sherman | Created from the clean-clone L0-L2 bench run at `9bbb26a`. |
| 2026-09-21 | OpenCode (GPT-5.6 Terra) + Sherman | Recorded failed independent-clone attempts on the Orin; no bench result exists from that machine. |
