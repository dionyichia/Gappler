# T1.2: Remaining Safety Fixes (B1 + I1)

**Session 2026-09-22, Sherman's Mac (off-box; no hardware touched).**

**Shared task:** task `T1.2` in [`../task-tree.html`](../task-tree.html).
**Record role:** Sherman-owned evidence. The task tree remains authoritative for task status.
Dion (task owner) was aware of this work before it started.

The stop-program third (B2/B2a/B2c) was already fixed 2026-09-21. Two items remained; both are
closed here. No arm, base, or ROS process was launched.

## B1: the launcher's phantom `q` key — fixed by rewording

`main.py` printed "press 'q' for emergency stop" (docstring + startup print) but contained no
keyboard handling at all — only the SIGINT handler. Worse, the wording called launcher-process
teardown an "emergency stop", which never touches the arm.

Fix (2 lines, zero behavior change): both lines now say Ctrl-C shuts down the launchers and point
at `arm/estop/` in its own terminal for the real arm stop. Implementing a `q` key was rejected:
~25 lines of terminal handling for a convenience alias to the identical `shutdown()`, days before
first power, while the wording would have stayed misleading anyway.

Verified by grep: no `press 'q'` promise remains in any owned launcher.

## I1: the double driver launch — deleted, with a regression test

`launchers/grasp_orchestrator.py:69-74` launched `arm_bringup.launch.py` at startup, then chained
into `launchers/start_grasp_pipeline.py`, which launched it again (`:100-105`) — two `rm_driver`
instances on one arm. Per the recorded decision, the grasp launcher owns bring-up: the orchestrator
copy is deleted. The dormant commented-out copy (`grasp_orchestrator.py:106-111`) is intentionally
left alone; the new check below guards its resurrection.

TDD, red-green:

- New L0 check `arm-bringup-single-launch` (`bench/static.py`): AST scan asserting
  `arm_bringup.launch.py` is named from exactly one owned site (AST, so comments don't count;
  skips its own reference in `bench/static.py`).
- RED: failed with 2 sites (`grasp_orchestrator.py:71`, `start_grasp_pipeline.py:102`).
- GREEN after the deletion: 1 site, all 11 L0 checks clean, L1 contracts clean
  (0 warnings), `./bench/run.sh quick` PASS on the Mac.

## Interpretation

`[observed]` T1.2 is complete: all three safety-fix items (stop program, launcher wording, single
driver launch) are evidenced. First power (T1.6) still needs T1.4 (physical setup, Sherman, in
progress) and the Ctrl-C solo-operator discipline recorded for T1.5.

## Changelog

| Date | Who | Change |
|---|---|---|
| 2026-09-22 | OpenCode (Muse Spark) + Sherman | Created: B1 reworded (2 lines), I1 orchestrator copy deleted behind a new failing-first L0 check. T1.2 closed. |
