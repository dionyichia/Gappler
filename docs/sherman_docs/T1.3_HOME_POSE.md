# T1.3: Home Pose Decision Evidence

**Sessions 2026-09-22, Sherman (Mac + lab box) + Dion (owner, aware).**

**Shared task:** task `T1.3` in [`../task-tree.html`](../task-tree.html).
**Record role:** Sherman-owned evidence. The task tree remains authoritative for task status.

Decision (Dion, 2026-09-20, T1.1/T0.7 — not re-decided here): use the `realman_manip` row,
recalibrate first, trust neither set. Recalibration scope (Sherman, 2026-09-22): T1.3 closes on
code + sim + docs; live-joint verification is a T1.7 precondition, not a T1.3 step.

| Joint | Code before (`main` row) | Decided (`realman_manip` row) |
|---|---|---|
| joint1 | 0.0 | -0.0175 |
| joint2 | 0.0 | -0.1745 |
| joint3 | 0.7854 | 0.7854 (only unchanged) |
| joint4 | 0.0 | **-3.0718** (176° difference) |
| joint5 | 1.5708 | -1.6930 |
| joint6 | 1.5708 | -1.6057 |

## Pre-change baseline 2026-09-22 (banked before any edit)

Lab box, isolated worktree, Sherman direct runs:

- `sim_moveit.sh`: home (main) 0.0044, home (realman_manip) 0.0047, zero 0.0049 rad — both
  candidate poses reachable and collision-free in the model.
- `state_machine_sim.sh`: PASS (one designed C7 expected-FAIL); startup homes to `main`'s row
  within 0.0001 rad. A post-test `move_group` teardown SIGSEGV in the launch log is benign noise
  (test had already passed).
- Post-change acceptance: same errors within ~0.002 rad AND the homing line reading the
  realman_manip row.

## Stale-warning finding (verified absent)

T1.3 says the safety warning "still quotes the old pose". Repo-wide value grep on current `dev`:
the old row appears only in the `CODE_AUDIT` history table (correct to keep), the bench fixtures
(intentional), and the startup guide quotes the *decided* row (correct). `ORIENTATION.md` §8.1
describes unprompted homing behavior with no joint values at all. **No stale-values quote exists
to fix** — recorded here instead of manufacturing a doc edit.

## Out of scope (confirmed)

- `trivial_mtc.cpp:73` private home-pose copy: left untouched (dead code path by T0.4 safety
  decision; editing implies maintenance).
- No real-arm contact; no recalibration step (deferred to T1.7 by scope decision above).

## Control result 2026-09-22: FAIL tracks the home values (landing STOPPED)

Pristine-`dev` control worktree (`~/rcp-t13ctl`, `d8c6e00`, own overlay build): `state_machine_sim.sh`
**PASS**, final approach commands the gripper close, homing to `main`'s row. Same box, same
arm-off-network state, same scripts and mocks as the branch runs.

| Tree | Home row | Final Cartesian approach |
|---|---|---|
| `dev` control | `main` | PASS (close commanded) |
| `t1.3-home-pose` (2 runs) | realman_manip | FAIL (Cartesian 25% trajectory, worker exits) |

`[observed]` The FAIL is pose-dependent, not environmental. The flipped elbow (joint4 0.0 →
-3.0718) changes the approach's end configuration branch, from which the straight-line final has
no valid completion. **T1.3 does not merge on this evidence** — the decided pose breaks the grasp
approach in the model. Escalated to Dion: the T1.1 pose decision reopens (adjust the approach
planner vs revisit the row). `sim_moveit` stays green on both rows (joint-space reachability is
not the issue; the Cartesian final is).

## Scenario sweep 2026-09-22 (same box, arm off-network throughout)

Env knobs (`SM_CY`, `SM_FAR`, defaults = baseline) added in `test_state_machine_sim.py`; no
rebuild needed (test-only). All runs home to the decided row @ 0.0001 rad, 0 retries:

| Variant | Scenario delta | Final approach |
|---|---|---|
| V0 (repeat) | none | FAIL (identical to the 2 prior branch runs) |
| V1 | centroid +80 px lateral | FAIL |
| V2 | start depth 0.30 → 0.22 m | FAIL |

5/5 branch FAILs vs 2/2 old-numbers PASSes (baseline + pristine-`dev` control). The lateral shift
(V1) may only rigid-shift the geometry, but the shortened path history (V2) still fails — the
finding stands systematic, not scenario-narrow. Box verified clean after every run.

## Conviction 2026-09-22: joint4 at the hard limit (scratch probe, never merged)

One-line scratch print in a disposable `~/rcp-j4probe` worktree (removed after; probe code in no
branch, no PR): joints at the failed final attempt were
`[0.009, -0.179, 0.985, -3.092, -1.498, -1.6]`.

- joint4 = **-3.092 vs lower limit -3.1: 0.008 rad (0.5°) of room.** Every other joint comfortable
  (nearest: joint5 margin 0.735).
- The approach itself consumed the home margin: joint4 walked -3.0718 (home) → -3.092 (final
  attempt), leaving 0.008; the remaining line needs more negative → invalid step → 25% stop.
- `[observed]` Prime suspect convicted by direct measurement. The failure is a joint-limit wall,
  not singularity or scene collision. Fix families, cheapest first: nudge joint4 off the limit
  (Option A) vs planner-side robustness (Option B); revert (Option C) stays parked.

## Changelog

| Date | Who | Change |
|---|---|---|
| 2026-09-22 | OpenCode (Muse Spark) + Sherman | Created with banked pre-change baseline; stale-warning verified absent; scope answers recorded. |
| 2026-09-22 | OpenCode (Muse Spark) + Sherman | Control verdict: pristine-`dev` PASS vs branch FAIL×2 on the final Cartesian approach — pose-dependent finding confirmed, landing stopped, escalated to Dion. |
| 2026-09-22 | OpenCode (Muse Spark) + Sherman | Conviction: joints-at-failure print shows joint4 at -3.092, 0.008 rad from its -3.1 limit. Fix families: joint4 nudge (A) vs planner robustness (B); revert (C) parked. |
