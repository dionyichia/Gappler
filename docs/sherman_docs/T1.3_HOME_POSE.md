# T1.3: Home Pose Decision Evidence

**Sessions 2026-09-22, Sherman (Mac + lab box) + Dion (owner, aware).**

**Shared task:** task `T1.3` in [`../task-tree.html`](../task-tree.html).
**Record role:** Sherman-owned evidence. The task tree remains authoritative for task status.

Decision (Dion, 2026-09-20, T1.1/T0.7 — not re-decided here): use the `realman_manip` row,
recalibrate first, trust neither set. Recalibration scope (Sherman, 2026-09-22): T1.3 closes on
code + sim + docs; live-joint verification is a T1.7 precondition, not a T1.3 step.

| Joint | Code before (`main` row) | Decided (`realman_manip` row) |
|---|---|---|
| joint1 | 0.0 | -0.0175 |
| joint2 | 0.0 | -0.1745 |
| joint3 | 0.7854 | 0.7854 (only unchanged) |
| joint4 | 0.0 | **-3.0718** (176° difference) |
| joint5 | 1.5708 | -1.6930 |
| joint6 | 1.5708 | -1.6057 |

## Pre-change baseline 2026-09-22 (banked before any edit)

Lab box, isolated worktree, Sherman direct runs:

- `sim_moveit.sh`: home (main) 0.0044, home (realman_manip) 0.0047, zero 0.0049 rad — both
  candidate poses reachable and collision-free in the model.
- `state_machine_sim.sh`: PASS (one designed C7 expected-FAIL); startup homes to `main`'s row
  within 0.0001 rad. A post-test `move_group` teardown SIGSEGV in the launch log is benign noise
  (test had already passed).
- Post-change acceptance: same errors within ~0.002 rad AND the homing line reading the
  realman_manip row.

## Stale-warning finding (verified absent)

T1.3 says the safety warning "still quotes the old pose". Repo-wide value grep on current `dev`:
the old row appears only in the `CODE_AUDIT` history table (correct to keep), the bench fixtures
(intentional), and the startup guide quotes the *decided* row (correct). `ORIENTATION.md` §8.1
describes unprompted homing behavior with no joint values at all. **No stale-values quote exists
to fix** — recorded here instead of manufacturing a doc edit.

## Out of scope (confirmed)

- `trivial_mtc.cpp:73` private home-pose copy: left untouched (dead code path by T0.4 safety
  decision; editing implies maintenance).
- Driver feedback only during T1.3 (zero motion commanded; state machine never launched);
  no recalibration step (deferred to T1.7 by scope decision above).

## Control result 2026-09-22: FAIL tracks the home values (landing STOPPED)

Pristine-`dev` control worktree (`~/rcp-t13ctl`, `d8c6e00`, own overlay build): `state_machine_sim.sh`
**PASS**, final approach commands the gripper close, homing to `main`'s row. Same box, same
arm-off-network state, same scripts and mocks as the branch runs.

| Tree | Home row | Final Cartesian approach |
|---|---|---|
| `dev` control | `main` | PASS (close commanded) |
| `t1.3-home-pose` (2 runs) | realman_manip | FAIL (Cartesian 25% trajectory, worker exits) |

`[observed]` The FAIL is pose-dependent, not environmental. The flipped elbow (joint4 0.0 →
-3.0718) changes the approach's end configuration branch, from which the straight-line final has
no valid completion. **T1.3 does not merge on this evidence** — the decided pose breaks the grasp
approach in the model. Escalated to Dion: the T1.1 pose decision reopens (adjust the approach
planner vs revisit the row). `sim_moveit` stays green on both rows (joint-space reachability is
not the issue; the Cartesian final is).

## Scenario sweep 2026-09-22 (same box, arm off-network throughout)

Env knobs (`SM_CY`, `SM_FAR`, defaults = baseline) added in `test_state_machine_sim.py`; no
rebuild needed (test-only). All runs home to the decided row @ 0.0001 rad, 0 retries:

| Variant | Scenario delta | Final approach |
|---|---|---|
| V0 (repeat) | none | FAIL (identical to the 2 prior branch runs) |
| V1 | centroid +80 px lateral | FAIL |
| V2 | start depth 0.30 → 0.22 m | FAIL |

5/5 branch FAILs vs 2/2 old-numbers PASSes (baseline + pristine-`dev` control). The lateral shift
(V1) may only rigid-shift the geometry, but the shortened path history (V2) still fails — the
finding stands systematic, not scenario-narrow. Box verified clean after every run.

## Conviction 2026-09-22: joint4 at the hard limit (scratch probe, never merged)

One-line scratch print in a disposable `~/rcp-j4probe` worktree (removed after; probe code in no
branch, no PR): joints at the failed final attempt were
`[0.009, -0.179, 0.985, -3.092, -1.498, -1.6]`.

- joint4 = **-3.092 vs lower limit -3.1: 0.008 rad (0.5°) of room.** Every other joint comfortable
  (nearest: joint5 margin 0.735).
- The approach itself consumed the home margin: joint4 walked -3.0718 (home) → -3.092 (final
  attempt), leaving 0.008; the remaining line needs more negative → invalid step → 25% stop.
- `[observed]` Prime suspect convicted by direct measurement. The failure is a joint-limit wall,
  not singularity or scene collision. Fix families, cheapest first: nudge joint4 off the limit
  (Option A) vs planner-side robustness (Option B); revert (Option C) stays parked.

## Rest readings 2026-09-22 (zero-motion driver session, `[reported]`)

During the T1.3 work, the arm driver was connected for joint feedback only: `/joint_states`
steady at ~200 Hz, **zero motion commanded, state machine never launched, nothing moved**
(Sherman, confirmed 2026-09-23).

- Rest joints approximately `[0.0143, 0.0006, 0.0107, 0.0014, 0.0014, 0.0016]` rad.
- `[inferred]` This is the arm's straight-up transport/candle posture, not a candidate home pose.
  Consistent with the as-found posture observed later; corroborates (not proves) nothing has moved
  it since.

## Final status 2026-09-22: original row retained in `dev`

`dev` still uses the original `main` home row. The `realman_manip` row was tested only in the
unmerged evidence branch `t1.3-home-pose` / PR #18.

Final results:

- Original row: 2/2 full simulated grasp cycles PASS.
- `realman_manip` row: 5/5 final Cartesian approaches FAIL at about 25% trajectory completion.
- Joints at failure included joint4 at `-3.092`, only `0.008` rad from its `-3.1` limit.
- The failure is therefore a joint-limit-margin failure, not proof of singularity or scene collision.

Next steps:

- Do not merge PR #18.
- Complete T1.4 physical safety setup without arm motion.
- Define a purpose-built candidate pose with real joint-limit margin.
- Validate the candidate with `sim_moveit.sh` and the full `state_machine_sim.sh` matrix.
- Keep T1.3 marked PROGRESS until T1.4 and a validated replacement pose are done.

## Purpose-built candidates 2026-09-23 (FK screen, box sim owed)

Branch `t1.3-purpose-built-home` from `origin/dev` @ `f7da799`. Mount truth from
T1.4 DONE: flange 0.18 fwd, 0.48 above RBL, yaw pi. Table 0.80 provisional per
Sherman. RBL floor height 0.10 assumed and flagged. Scratch FK (`/tmp/t13_fk.py`,
stdlib, URDF chain) cross-checked: tool z matches the banked Link6 figures
(MAIN 1.123, rejected 1.148).

| Candidate | Row | Min home margin | FK screen |
|---|---|---|---|
| C1 = MAIN row (control) | [0, 0, 0.7854, 0, 1.5708, 1.5708] | 0.662 (j5), PASS | link origins FK-screened; tool 0.323 above the provisional table height pointing fwd-down 45 deg, plausible observation posture |
| C2 tucked (challenger) | [0, -0.20, 0.7854, 0, 1.35, 1.5708] | 0.883 (j5), PASS | tool 0.395 above provisional table height but pointing mostly forward (21 deg down), weaker view angle, taller |

`[observed]` Nothing in the evidence faults the MAIN row: 2/2 PASS, margins pass,
posture plausible. C2 buys margin but spends view angle and height. What the FK
screen does not show: link origins are not collision meshes, and the table
position in the robot frame is unknown (T1.4), so containment and clearance stay
unproven until Round A records placement and Round B proves behavior.
Recommendation: adopt C1 as final pending box re-confirmation on this branch,
keep C2 as the sim challenger. Box runs owed before any pin flip:
`sim_moveit.sh` both rows, full `state_machine_sim.sh` matrix on C1, 10x
repeat-homing, margin assertion in CI. Test-only changes on this branch so far:
C2 probe row in sim POSES plus the TF placement record; `HOME_JOINTS` untouched.

## Final verdict 2026-09-23: MAIN row adopted (sim-validated + documented)

Decision (Sherman, 2026-09-23, on this branch's evidence): the final home pose is the
MAIN row `[0, 0, 0.7854, 0, 1.5708, 1.5708]`, already `HOME_JOINTS` in `dev`, so no
production-code change lands with this decision.

Evidence gathered today (lab box, `t1.3-home-pose-select`, arm off-network, mock only):

- Round A placement (`sim_moveit.sh` + TF record, `d85ba90`): 4/4 reachable; TF-measured
  link origins match the independent FK within millimetres on every axis (mount truth
  confirmed against the live model). C1: tool (0.429, 0, 1.025) robot-base frame, view
  44 deg down, grasp point lowest (0.922). C2: tool higher (1.095), view 21 deg down.
  Zero stares at the ceiling. C1 wins placement; C2's margin edge (0.883 vs 0.662) buys
  nothing placed. Both clear the 0.20 home bar.
- Round B behavior (`state_machine_sim.sh` on C1): startup homing 0.0001 rad, approach
  steps move the arm, EXECUTING entered, final approach completes with gripper close
  commanded — the exact step that killed the rejected row 5/5. Safety walls accepted.
- One FAIL, attributed: return-pose step 0.0563 rad off because the test hardcoded the
  old RETURN joint3 (2.3562) while PR #26 fixed the code to 2.3000 (delta 0.0562 — the
  arm went to the right pose; the test compared a stale number). Fixed test-only on this
  branch; box green re-run owed to close the loop.
- RViz viewing on the box monitor corroborated posture (elbow up, tool down-forward).
- Variant matrix 2026-09-24 (`t1.3-variant-matrix`, `SM_CY`/`SM_FAR` knobs, box sim,
  arm off-network): baseline PASS, lateral (`SM_CY=80`) PASS, shortened
  (`SM_FAR=0.22`) PASS — full cycle with gripper close in all three, return pose
  0.0001 rad, C7 XFAIL as designed. The T1.3 scenario residual is closed; MAIN
  survives off-center and shortened geometries.
- Explicitly not proven (residuals, not blockers of this sim decision): T1.1
  supersession owed (Dion), live-joint verification + supervised move (T1.7), D3
  stop-figure transcription, table frame (T1.4-cell). This merge certifies
  sim-validated + documented, verbatim.

## Changelog

| Date | Who | Change |
|---|---|---|
| 2026-09-22 | OpenCode (Muse Spark) + Sherman | Created with banked pre-change baseline; stale-warning verified absent; scope answers recorded. |
| 2026-09-22 | OpenCode (Muse Spark) + Sherman | Control verdict: pristine-`dev` PASS vs branch FAIL×2 on the final Cartesian approach — pose-dependent finding confirmed, landing stopped, escalated to Dion. |
| 2026-09-22 | OpenCode (Muse Spark) + Sherman | Conviction: joints-at-failure print shows joint4 at -3.092, 0.008 rad from its -3.1 limit. Fix families: joint4 nudge (A) vs planner robustness (B); revert (C) parked. |
| 2026-09-22 | OpenCode (Muse Spark) + Sherman | Final status: original row retained in dev; PR #18 stays unmerged; T1.3 remains PROGRESS pending T1.4 and a validated replacement pose. |
| 2026-09-23 | OpenCode (Muse Spark) + Sherman | Purpose-built candidates on fresh branch: C1 MAIN row recommended (margins + posture + 2/2 history), C2 tucked as challenger; FK-screened locally, box sim owed, no code changed. |
| 2026-09-23 | OpenCode (Muse Spark) + Sherman | Round A decides C1 on placement (TF vs FK millimetre match); Round B proves C1 behavior (approach + close PASS, return FAIL attributed to stale test RETURN, fixed test-only); verdict MAIN final with residuals; C2 probe reverted; archive guide bannered. |
| 2026-09-23 | OpenCode (Muse Spark) + Sherman | Corrected scope line (driver feedback only, zero motion — not "no contact") and entered the T1.3 zero-motion rest readings with the straight-up candle interpretation. |
| 2026-09-24 | OpenCode (Muse Spark) + Sherman | Variant matrix on MAIN (SM_CY/SM_FAR knobs, box sim): baseline + lateral + shortened all PASS with close commanded; scenario residual closed. Fixed stale "unvalidated on the real arm" test string (T1.7 validated 2026-09-23). |
